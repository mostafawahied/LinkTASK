import 'package:flutter/material.dart';

class EMagazineScreen extends StatelessWidget {
  const EMagazineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("E-Magazine"),
    );
  }
}
